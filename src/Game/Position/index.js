import React from "react";

const Position = (props) => {
  //Can be 3 type: Food, Body or Empty
  let position = () => {
    //if props.data.isFood, etc....

    return (
      <div
      className="empty">
        p
      </div>
    )
    
  }
  return position()
}
export default Position;